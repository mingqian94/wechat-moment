# Python
.venv/
__pycache__/
*.pyc
*.pyo

# 运行时生成
activation.dat
错误截图/

# 环境变量（含私钥，绝对不提交）
.env

# 模板图：默认模板已打包进仓库（见 templates/），通常不用自己截
# 如果在某台机器上重新校准过，新的模板图会以"已修改"出现，正常提交覆盖即可

# macOS
.DS_Store

# 本地工具配置（非项目内容）
.claude/

# 从网上下载的真实测试图（第三方版权图片，不是项目自己生成的素材，不提交）
test_images_real/
