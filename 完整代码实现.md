# 朋友圈助手 — 代码实现说明

> Python 3.12 实现，跨平台开发（Mac），Windows 发布
> MVP 范围：图片发布 + 多账号调度 + 注册码授权
> 版本：v1.3 | 日期：2026-06-17

---

## 一、项目结构

```
wechat-moment/
├── src/
│   ├── main.py               ← 入口，App 类串联全部模块
│   ├── auth.py               ← 注册码验证、激活文件读写
│   ├── window_manager.py     ← 微信窗口查找与激活（Windows/Mac 分支）
│   ├── image_recognition.py  ← OpenCV 模板匹配，FindAndClick
│   ├── publisher.py          ← 单条发布完整流程
│   ├── scheduler.py          ← 时间表生成、养号策略、调度循环
│   └── gui/
│       ├── __init__.py
│       ├── activation.py     ← 激活弹窗
│       ├── add_task.py       ← 添加任务弹窗
│       └── main_window.py    ← 主界面
├── templates/                ← 按钮截图模板（在目标机器截图一次）
│   ├── moments_btn.png       ← 朋友圈入口按钮
│   ├── camera_btn.png        ← 相机图标
│   ├── album_btn.png         ← 从相册选择
│   └── post_btn.png          ← 发表按钮
├── .venv/                    ← Python 虚拟环境（不提交）
├── activation.dat            ← 注册码激活记录（程序生成）
├── requirements.txt
└── README.md
```

---

## 二、模块说明

### auth.py

注册码的生成与验证，核心逻辑：

```
生成：SHA256(私钥 + 前缀 + 序号) → 取前24位 → 每6位加横线
格式：BAS-XXXXXX-XXXXXX-XXXXXX-XXXXXX
验证：穷举序号 1-9999 重算哈希，命中则通过
```

| 函数 | 说明 |
|------|------|
| `is_activated()` | 检查 activation.dat 是否存在且格式有效 |
| `verify_code(code)` | 验证注册码，通过则写入 activation.dat，返回 `{ok, version}` |
| `save_activation(code, version)` | 写激活文件 |
| `get_version()` | 读已激活版本（基础版 / 标准版） |
| `generate_batch(count, prefix)` | **开发者工具**，批量生成注册码，不随程序分发 |

私钥通过环境变量 `WM_PRIVATE_KEY` 注入，开发时在 `.env` 设置，发布前编译进 exe。

---

### window_manager.py

平台分支：

| 平台 | 实现 |
|------|------|
| Windows | `ctypes` 调用 `EnumWindows`，按类名 `WeChatMainWndForPC` 查找 |
| Mac（开发） | 返回 3 个 mock 窗口，`activate_window` 只打印日志 |

| 函数 | 说明 |
|------|------|
| `find_wechat_windows()` | 返回 `[{hwnd, pid, title}]` |
| `activate_window(hwnd)` | 置顶窗口，返回是否成功 |
| `get_window_rect(hwnd)` | 返回 `(x, y, w, h)` |
| `bind_aliases(windows)` | 给窗口分配 A1/A2/A3 别名 |

---

### image_recognition.py

OpenCV 模板匹配，支持多尺度容错（0.8×、0.85×、0.9×、1.0×、1.1×、1.15×）：

| 函数 | 说明 |
|------|------|
| `find_template(name, region)` | 在 region 内查找模板，返回屏幕绝对坐标 `(x, y)` 或 None |
| `find_and_click(name, region, timeout)` | 循环查找并点击，超时返回 False |
| `take_screenshot(region, save_path)` | 截图保存（错误时调用） |

匹配阈值 `MATCH_THRESHOLD = 0.80`，可在文件顶部调整。
模板图存放在 `templates/` 目录，首次使用需在目标机器上截图。

---

### publisher.py

单条发布流程（7 步）：

```
激活窗口 → 点朋友圈 → 点相机 → 点相册选择
→ 文件选择对话框输入图片路径 → 剪贴板粘贴文案 → 点发表
```

Mac 开发模式下跳过所有实际操作，1.5 秒后直接返回成功。
错误截图保存至 `错误截图/error_时间戳_步骤.png`。

---

### scheduler.py

**养号策略：**

| 级别 | 注册天数 | 日上限 | 小时上限 | 最小间隔 |
|------|---------|--------|---------|---------|
| S | < 7 天 | 2 条 | 1 条 | 360 分钟 |
| A | 7-30 天 | 5 条 | 2 条 | 40 分钟 |
| B | ≥ 30 天 | 8 条 | 3 条 | 25 分钟 |

**随机化：**
- 分钟取 03-12 / 18-27 / 33-42 / 48-57（避开整点/半点）
- 最小间隔 ±20% 抖动
- 用户指定时间加 ±5 分钟抖动

**Scheduler 类：** 后台线程执行，通过回调更新 GUI，连续 3 条失败自动停止。

---

### gui/

| 文件 | 说明 |
|------|------|
| `activation.py` | 激活弹窗，输入注册码，调用 `verify_code` |
| `add_task.py` | 添加任务弹窗，选图片（filedialog）、输文案、选时间和类型 |
| `main_window.py` | 主界面，账号状态卡片 + 任务列表（ttk.Treeview）+ 日志滚动框 |

GUI 与调度线程通过 `queue.Queue` 解耦（线程安全）。

---

## 三、数据流

```
用户操作（GUI）
    ↓
App（main.py）
    ├─ on_add_task → AddTaskDialog → main_win.add_task(task)
    ├─ on_generate → generate_schedule(tasks, accounts) → main_win.update_schedule()
    └─ on_start → Scheduler.start()
                      ↓（后台线程）
                  execute_publish(task)
                      ├─ activate_window(hwnd)
                      ├─ find_and_click(各模板)
                      └─ 回调 → queue → main_win 更新状态/日志
```

---

## 四、配置与环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `WM_PRIVATE_KEY` | 注册码私钥 | `WM_DEFAULT_KEY_2026`（仅开发用） |

开发时在项目根目录新建 `.env`：
```
WM_PRIVATE_KEY=你的私钥
```

发布时将私钥硬编码进 exe（PyInstaller 打包前替换）。

---

## 五、Windows 部署补充

1. 在 Windows 目标机器上安装 Python 3.12 并建 venv
2. `pip install -r requirements.txt`
3. 打开微信多开工具，登录所有账号
4. 截图 `templates/` 里的 4 张按钮模板（在标准 DPI 下截，不缩放）
5. 运行 `python src/main.py`，输入注册码激活

---

*代码版本：v1.3*
*更新日期：2026-06-17*
